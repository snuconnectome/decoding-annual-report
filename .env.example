# 환경변수 템플릿 — 저장소에 커밋됨
# 실제 값은 이 파일을 `.env.local` 로 복사 후 값 입력.
# `.env.local` 은 `.gitignore` 로 제외됨.

# Gemini nanobanana2 (gemini-3.1-flash-image-preview) 슬라이드 생성
# 발급: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GOOGLE_API_KEY=
